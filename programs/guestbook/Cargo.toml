[package]
name = "guestbook"
version = "0.1.0"
description = "Permanent on-chain guestbook"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "guestbook"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.32.1"
